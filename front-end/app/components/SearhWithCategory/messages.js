/*
 * SearhWithCategory Messages
 *
 * This contains all the text for the SearhWithCategory component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SearhWithCategory';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SearhWithCategory component!',
  },
});
