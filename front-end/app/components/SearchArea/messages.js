/*
 * SearchArea Messages
 *
 * This contains all the text for the SearchArea component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SearchArea';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SearchArea component!',
  },
});
