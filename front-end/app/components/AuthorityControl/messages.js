/*
 * LocationControl Messages
 *
 * This contains all the text for the LocationControl component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.LocationControl';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LocationControl component!',
  },
});
