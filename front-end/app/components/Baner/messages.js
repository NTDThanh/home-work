/*
 * Baner Messages
 *
 * This contains all the text for the Baner component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Baner';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Baner component!',
  },
});
