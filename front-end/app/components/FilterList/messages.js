/*
 * FilterList Messages
 *
 * This contains all the text for the FilterList component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.FilterList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FilterList component!',
  },
});
