/*
 * ListCourse Messages
 *
 * This contains all the text for the ListCourse component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ListCourse';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ListCourse component!',
  },
});
