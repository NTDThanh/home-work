/*
 * CustomSlider Messages
 *
 * This contains all the text for the CustomSlider component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CustomSlider';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CustomSlider component!',
  },
});
