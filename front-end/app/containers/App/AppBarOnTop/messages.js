/*
 * AppBarOnTop Messages
 *
 * This contains all the text for the AppBarOnTop container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AppBarOnTop';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AppBarOnTop container!',
  },
});
