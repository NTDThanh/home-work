/*
 * UserLoginPage Messages
 *
 * This contains all the text for the UserLoginPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.UserLoginPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UserLoginPage container!',
  },
});
