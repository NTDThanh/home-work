// import { fromJS } from 'immutable';
// import { selectExerciseCreateEditPageDomain } from '../selectors';

describe('selectExerciseCreateEditPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
